# Reaction Standee

ショート動画向けの「立ち絵リアクション演出アプリ」です。Webカメラで大きなポーズを認識し、キャラクターの立ち絵差分をリアクションとして切り替えます。

口パク、音声入力、ホットキー操作はv1には入れていません。ユーザーの体を完全追従するのではなく、ポーズをキャラクター演出の入力コマンドとして扱います。

## 起動

```bash
npm install
npm run dev
```

設定画面:

```text
http://127.0.0.1:5173/settings
```

OBS・録画用の表示画面:

```text
http://127.0.0.1:5173/avatar
```

初回トラッキング時に、MediaPipe Pose LandmarkerのWASMとモデルをブラウザが読み込みます。

## v1機能

- MediaPipe Pose LandmarkerによるWebカメラポーズ認識
- 5種類のリアクション画像登録
- 通常時の微細な呼吸・浮遊・光量変化による生命感演出
- 登録画像の個別削除と全削除
- 画像切替時のCSSアニメーション
- キャラ画像のサイズ・位置調整
- 背景設定: 透明、グリーンバック、任意色
- 透明PNG向けの白フチ表示
- カメラ選択
- トラッキングON/OFF
- 判定感度: 低、標準、高
- `/settings` と `/avatar` の2画面構成
- localStorageによる設定保存
- IndexedDBによる画像保存

## 画像差分

設定画面から以下5枚を登録します。

```text
normal.png
joy.png
surprised.png
troubled.png
explain.png
```

推奨は背景透過PNGです。背景抜きは外部の画像編集アプリで行い、このアプリには透過済みPNGを登録してください。OBSでクロマキーを使う場合は、アプリ側の背景をグリーンバックに変更できます。

OBSのブラウザソースはChrome/Safariとは別のブラウザ保存領域を使うため、設定画面でアップロードした画像がOBS側に共有されない場合があります。その場合は、同じファイル名で `public/reactions/` にPNGを配置してください。アプリはブラウザ内のアップロード画像が無い時に、`/reactions/normal.png` などの静的ファイルを自動で読みます。

## 生命感演出

まばたき用の目元オーバーレイは、絵柄によって開いた目と閉じ目が同時に見えやすいため採用していません。代わりに、通常状態ではキャラ全体にごく小さな呼吸、浮遊、光量変化を入れて、目元を壊さずに軽い生命感を足しています。

## 認識するポーズ

- `normal`: 手が下がっている、または特別なポーズをしていない
- `joy`: 両手を大きく上げる、ばんざいポーズ
- `surprised`: 片手または両手が顔の近くにある
- `troubled`: 片手が頭の近くにある。頭の傾きも補助的に見ます
- `explain`: 片手が胸から肩の高さに上がっている

## 安定化

ポーズ候補が短時間だけ揺れた場合は、すぐには切り替えません。

- confidence thresholdで弱い判定を保留
- debounceで同じ候補が続いた時だけ切り替え
- cooldownで連続切り替えを抑制
- 通常状態へ戻る時は少し長めに待機
- カメラから一瞬外れても即座にはnormalに戻さない

## OBSで使う

1. `npm run dev` で起動します。
2. Chrome/Safariで `/settings` を開き、画像、カメラ、背景、サイズ、位置を設定します。
3. OBSで表示する画像は、必要に応じて `public/reactions/` に `normal.png` などの名前で配置します。
4. Chrome/Safari側の `/settings` でトラッキングをONにします。
5. OBSのブラウザソースに `/avatar` を指定します。
6. 背景透過が扱いづらい場合は、設定画面でグリーンバックを選びOBS側でクロマキーを使います。

OBSのブラウザソースではカメラ権限が拒否されることがあるため、`/avatar` ではカメラを起動しません。ポーズ認識はChrome/Safariの `/settings` で行い、現在のリアクション状態をローカルAPI `/api/reaction` 経由でOBSの `/avatar` に共有します。

## 今後の拡張メモ

- ポーズ条件の詳細調整UI
- 反応ごとのアニメーション選択
- リアクションごとの保持時間
- シーン別プリセット
- モデルファイルのローカル同梱
- 録画画角プリセット
